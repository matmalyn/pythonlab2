import math

op = "t"
while op != "n":  
    dane = input("Podaj 3 boki trójk¹ta (oddzielone przecinkami): ")

    lista = []  
    for x in dane.split(","):
        lista.append(int(x))  
    a, b, c = lista 

    print("Podano boki: ", a, b, c)

    if a + b > c and a + c > b and b + c > a: 
        print("Z podanych boków mo¿na zbudowaæ trójk¹t.")
       
        if (a**2 + b**2 == c**2 or
                a**2 + c**2 == b**2 or
                b**2 + c**2 == a**2):
            print("Do tego prostok¹tny!")

     
        print("Obwód wynosi:", (a + b + c))
        p = 0.5 * (a + b + c)

        P = math.sqrt(p * (p - a) * (p - b) * (p - c))
        print("Pole wynosi:", P)
        op = "n"
    else:
        print("Z podanych odcinków nie mo¿na utworzyæ trójk¹ta prostok¹tnego.")
        op = input("Spróbujesz jeszcze raz (t/n): ")

print("Program zakonczyl dzialanie")

*Napisz program, który na podstawie danych pobranych od u¿ytkownika, czyli 
*d³ugoœci boków, sprawdza, czy da siê zbudowaæ trójk¹t i czy jest to trójk¹t 
*prostok¹tny. Je¿eli da siê zbudowaæ trójk¹t, nale¿y wydrukowaæ jego obwód i 
*pole, w przeciwnym wypadku komunikat, ¿e nie da siê utworzyæ trójk¹ta.